{
  "name": "client",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.ts",
  "author": "",
  "license": "ISC",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf dist && tsc --build .",
    "start": "tsc --watch"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^3.8.3"
  }
}
